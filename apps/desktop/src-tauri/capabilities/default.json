{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "VYBZ Desktop Windows alpha — core + opener",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
