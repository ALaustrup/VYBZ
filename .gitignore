node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
.vercel
.env
.env.local

# Secrets — never commit
*.secrets
.env.secrets
**/.env.secrets

# Supabase CLI local state
supabase/.temp/

# Env: ignore secrets; keep committed templates
.env*.local
.env
.env.*
!.env.example

# Large encoded / master visuals — serve from Supabase `site-visuals` bucket.
# Keep tiny posters/previews + READMEs in git; upload loops via:
#   npm run visuals:upload
vizualz/
Vizualz/
public/backdrop/*.mp4
public/backdrop/*.webm
public/vdock/visuals/**/loop.mp4
public/vdock/visuals/**/loop.webm
.agents/
skills-lock.json

# AI review live-walk screenshots (paths referenced from run markdown)
docs/ai-review/runs/assets/

# Phase 11 perf artifacts
.lighthouseci/
dist/stats.html
test-results/
playwright-report/

# iOS Cap build outputs (ios/.gitignore covers App/public)
ios/build/
ios/App/build/
ios/DerivedData/
