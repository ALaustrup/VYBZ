/**
 * Drop banner visualizer — WebGL2 DropStage with Canvas2D seeded fallback.
 * Kept as the public import name for TrackCard / GlobalPlayer / Feed.
 */
export { DropStage as TrackVisualizer, DropStage } from "@/components/DropStage";
