/**
 * Frozen — not mounted by SuiteShell.
 *
 * Was the narrow-viewport pill row (Home / Finalize / Discover / Library / Messages).
 * Destinations live in the quiet app bar (Search / command palette) and owner
 * controls. SuiteAppRail / PrimaryRail / this file stay in-tree; do not re-import.
 */
export function MobileNav() {
  return null;
}
