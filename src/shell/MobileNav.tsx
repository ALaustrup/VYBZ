/**
 * Frozen — not mounted by SuiteShell.
 *
 * Was the narrow-viewport pill row (Home / Finalize / Discover / Library / Messages).
 * Destinations live in PrimaryRail (desktop) and the command palette; suite apps use
 * SuiteAppRail / SuiteAppRailMobile. Kept in-tree for history; do not re-import.
 */
export function MobileNav() {
  return null;
}
