import type { AppNotification } from "@/types";

// MYVYB starts brand new — no seeded notifications. Real activity (Vybs, Fails,
// comments, messages) populates the bell live from the backend.
export const NOTIFICATIONS: AppNotification[] = [];
