> **HISTORICAL ONLY — NOT AUTHORITATIVE — DO NOT USE FOR CURRENT IMPLEMENTATION OR SEQUENCING.**

# Product briefs for unbuilt surfaces (2026)

Three briefs describing products that do not exist. Each declared itself an "authoritative
product brief" while its definition of done was largely unchecked, which made planned work
read as committed work.

| Brief | Reality at archival |
|---|---|
| `RELAY.md` | Distribution delivery. **No DSP delivery code exists** — zero references to DDEX, ERN or SFTP anywhere in the repository. `/relay` renders a placeholder. |
| `SENTINEL.md` | Secure prerelease rooms. Only the watermark edge functions exist; `/sentinel` renders a placeholder. |
| `COVERLAB.md` | Artwork readiness and repair. Only `visual-generate` exists; `/coverlab` renders a placeholder. Artwork DPI is never actually parsed. |

Relay's honest framing — *"no fake direct-DSP claims"* — survives as Law 7 and §11 of
[`VYBZ_MASTERPLAN.md`](../../../VYBZ_MASTERPLAN.md). External distribution is now an
explicitly gated milestone (M13/M14) rather than a brief.
